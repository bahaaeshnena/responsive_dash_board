class TrasnsctionModel {
  final String title, data, amount;
  final bool iswithdrawal;

  const TrasnsctionModel(
      {required this.title,
      required this.data,
      required this.amount,
      required this.iswithdrawal});
}
